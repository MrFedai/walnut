Dataset Availability: To adhere to the 1GB submission limit, the walnut leaf dataset is hosted externally. Please run python download\_dataset.py before executing train.py to automatically fetch the data.

